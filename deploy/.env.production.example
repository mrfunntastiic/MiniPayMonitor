# Copy file ini ke root project: cp deploy/.env.production.example .env.production
# Lalu isi nilai-nilainya sesuai konfigurasi VPS kamu

# === DATABASE ===
DATABASE_URL=postgresql://USERNAME:PASSWORD@localhost:5432/NAMA_DATABASE

# === SERVER ===
NODE_ENV=production
PORT=5000
